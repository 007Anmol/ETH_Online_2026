import { notImplemented } from "@/lib/api/http";

/** Saachi: generate a tap payload, then verify through POST /api/nfc/verify */
export async function POST() {
  return notImplemented("Saachi", "POST /api/nfc/simulate");
}
