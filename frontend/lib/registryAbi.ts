/**
 * @deprecated Use `registryAbi` from `@/lib/blockchain`.
 * This re-export is the Team 1 identity ABI (batch/mint/tag), not the old Team 2 shipment registry.
 */
export { registryAbi } from "./blockchain/contracts";
