{
  "name": "@verichain/subgraph",
  "private": true,
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "create-local": "graph create --node http://localhost:8020 verichain",
    "remove-local": "graph remove --node http://localhost:8020 verichain",
    "deploy-local": "graph deploy --node http://localhost:8020 --ipfs http://localhost:5001 --version-label v0.0.1 verichain"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.51.2",
    "@graphprotocol/graph-ts": "0.31.0"
  }
}
